# MeraSoftware Documentation Index

This folder is split into two groups:

- Active reference docs for the current codebase.
- Legacy docs kept for historical context only.

If you are trying to understand the app as it works today, start with `00_CURRENT_SYSTEM.md`.

## Active Docs

### `00_CURRENT_SYSTEM.md`
Current source of truth for routes, dashboards, headers, and login flow.

### `01_ARCHITECTURE_OVERVIEW.md`
Current frontend architecture, route layout, auth flow, and active shell components.

### `02_COMPONENT_GUIDE.md`
Current component guide for the active header, dashboard, and layout components.

### `03_DATA_FLOW_AND_PATTERNS.md`
State, API, and caching patterns used by the app.

### `04_BACKEND_OVERVIEW.md`
Backend structure, routes, models, and helpers.

### `05_QUICK_REFERENCE.md`
Fast lookup for files, routes, and common development tasks.

### `12_CLIENT_ACTIVITY_SORT_AUDIT.md`
Current audit and implementation routing for admin client working-activity sorting and the pending node-update write path.

### `13_PROJECT_CREATION_AND_APPROVAL_PLAN.md`
Verified project categories, product fields, customer purchase flow, admin `Website Management > Projects` UI scaffold, approval integration, and regression guardrails.

### `14_CODEBASE_AUDIT_INDEX.md`
Central AI handoff index containing the audited route/file/model map, real database evidence, current implementation state, pending work, and regression boundaries.

### `15_START_NEW_PROJECT_UI_HISTORY.md`
Current `/start-new-project` list-row UI (now live-wired to real product data via `/api/get-product` and `/api/product-details`), the shared `CustomerWorkspaceTabs` underline-tab component now used across the customer portal, named backup folders with restore instructions, rejected/reverted approaches, and the related sidebar/footer layout fix.

### `16_WALLET_AND_PROJECTS_STATUS_FIX.md`
Before/after record for the wallet page width fix (`max-w-6xl` -> `max-w-7xl`), the `ProjectsAndPlans` tab reduction (five tabs -> `All`/`Projects`/`Plans`), and the project-row status rewrite (`Booked` / `Developer Assigned` / `{progress}% Complete` / `Completed` / `Payment Rejected`), plus the confirmed evidence that no working developer-assignment backend exists yet.

### `17_ADD_PROJECT_FORM_AND_PERFECT_FOR_AUDIT.md`
Full history of `AdminCreateProjectPage.js` becoming a working save form: field-wiring, Additional Features dropdown (three iterations), the `products.isHidden` string-vs-boolean data bug fix, the full "Who is it for?" rebuild (keyboard-driven free-text + live text-suggestions + icon-grid, backed by the new `perfectForSuggestion` collection), the `startingNodeTitle` schema addition, Cloudinary image upload wiring, and the final `POST /api/upload-product` submit wiring. Also documents a real production incident — old products crashing after the `perfectFor` schema change, root-caused and fixed via a backup-first migration — and a separate **unfixed, documented-only** finding: the customer-facing "Customize Your Plan" section ignores a product's `additionalFeatures` selection and uses its own unrelated `compatibleWith`-category filter instead. Read this before touching `AdminCreateProjectPage.js`, `PerfectForField.js`, `perfectForIconSet.js`, `perfectForOptions.js`, `packageOptions.js`, `productModel.js`, or `ProductDetails.js`'s "Customize Your Plan" section.

### `18_PROJECT_DETAIL_PAGE_AND_HEADER_REWORK.md`
Records a rejected-then-reverted first attempt at an admin project detail page, the confirmed direction change (build/perfect the customer detail page first, reuse it for admin later via a thin wrapper page — not an `isAdminView` branch), the new shared `frontend/src/components/ProjectDetailView.js` component now used by `StartNewProjectDetail.js` (final section order: description -> what's included -> add-on features with checkboxes -> who is it for -> two proceed buttons, `?` info-tooltips for secondary detail, no price shown), its full style-iteration history (colorful cards -> rejected serif/paper look -> final site-consistent black-text/sans-serif look), and the dark-gradient-banner header now shared by `ProjectDetails.js`, `StartNewProjectDetail.js`, `ProjectsAndPlans.js`, and `StartNewProject.js`. Read this before touching any of those four files, before building the still-pending admin project detail/edit/delete page, or before wiring the two proceed buttons' real logic.

### `19_TYPOGRAPHY_STANDARDIZATION_AUDIT.md`
Full-site (not just customer dashboard) read-only audit of every text-size and text-color class across 161 `pages`/`components` files, done before any typography code changes. Records the approved 5-size type-scale (`text-2xl`/`text-xl`/`text-lg`/`text-base`/`text-sm`), the approved black/white-only text-color rule with `text-black` as canonical, the explicit exemption for status badge/pill semantic colors, the approved direct-remap approach for small badge sizes (`text-xs`/`text-[10px]`/`text-[11px]` -> `text-sm`), confirmation that `Practice.js`/`UserDemo.js` are live routed pages (`/practice`, `/demo`) and in scope, and the known risk areas (mixed body-vs-subtext use of `text-sm`, dark-background `gray-300`/`slate-300` needing `text-white` not `text-black`, one-off `text-6xl`/`text-[16px]`/`text-[17px]` values). Read this before making any font-size or text-color change anywhere in the frontend — implementation has not started yet.

### `20_PLAN_SYSTEM_AND_PLAN_DETAILS_PAGE.md`
Records the 27-file dead-page cleanup (moved to `pages/old-code-backup/`, nothing deleted), the `CustomerDashboard.js` "Start New Project" route fix, the plan-type-aware rebuild of `UserUpdateDashboard.js` (root-caused why the "Request Update" button stayed enabled on expired/recurring plans), the new `PlanDetails.js` subpage (plans now route to `/plan-details/:orderId` instead of `ProjectDetails.js`, which was explicitly left untouched), the one required `getOrderDetails.js` populate-field fix that makes recurring-plan detection work, and a confirmed-but-not-yet-fixed admin bug where marking one overdue invoice as paid resumes the whole plan even if a different, newer invoice on the same order is still overdue. Read this before touching `UserUpdateDashboard.js`, `PlanDetails.js`, `ProjectsAndPlans.js`'s `openDetails()`, `getOrderDetails.js`, or `invoiceLifecycle.js`.

### `21_PAYMENT_INVOICE_LEDGER_AUDIT_AND_FIX.md`
Root-causes the admin `Payment & Invoices` tab showing 2 unlinked records for one sale (7 paid invoices had no linked transaction — confirmed via live read-only DB audit, not assumption), fixes it going forward (new admin-only `monthlyInvoiceModel.internalNote` field, never shown to the customer; `AdminPaymentRecordDetail.js`'s "Mark Invoice Paid" renamed to "Record Payment" with a note field, distinct from the pre-existing customer-payment "Accept/Reject" verification action), and backfills the 7 existing orphaned records via the pre-existing `backend/scripts/auditPaymentInvoiceLedger.js --apply`. Also fully documents — but does **not** fix — that the entire customer-facing monthly-invoice payment system is broken/missing: `/my-invoices` (`UserInvoices.js`) calls a backend endpoint (`/api/my-invoices`) that does not exist anywhere in the codebase, its "Pay Now" button is a dead stub, and `PlanDetails.js`'s/`UserUpdateDashboard.js`'s "Payment overdue" notices show text only with no working link to the actual invoice. Read this before touching `AdminClientWorkspace.js`'s `PaymentInvoicesPanel`, `AdminPaymentRecordDetail.js`, `monthlyInvoiceController.js`, `invoiceLifecycle.js`, or before starting the still-pending customer invoice/payment feature.

### `22_MOBILE_SIDEBAR_DRAWER_AND_CHESS_SOCKET_AUDIT.md`
Two independent audits. (1) **Fixed**: `DashboardLayout.js`'s and `AdminLayout.js`'s full sidebars (`hidden ... lg:flex`) had no mobile equivalent at all — mobile users only got `SharedHeader.js`'s much shorter generic hamburger list. Fixed via a new shared `frontend/src/components/MobileSidebarDrawer.js` (presentation-only slide-in overlay) wired into both layouts, reusing the exact same `sidebarContent` markup/links as desktop — no duplicated nav data, `SharedHeader.js` untouched. (2) **Audited, not fixed**: chess (`frontend/src/chess/*`, `backend/chess/*`, previously undocumented anywhere) is the only feature using a live WebSocket instead of HTTP; on iPhone, buttons appear dead because Safari's ITP blocks the cross-site `sameSite:'None'` session cookie during the socket handshake (`backend/chess/chessSocket.js` auth relies solely on that cookie), and the UI never surfaces the resulting `connected`/`errorMessage` state to the user. Three candidate fixes were evaluated (client-exposed token fallback — rejected, weakens sitewide `httpOnly` protection; same-site reverse proxy — needs unverified hosting-level changes; separate chess-scoped low-privilege token — safest, but paused at user's request). Read this before touching either the two layout files or anything under `chess/`.

### `23_PAYMENT_SSOT_PHASE_0_TO_3.md`
Broad customer+admin payment/order ecosystem audit, then a user-approved phased plan to build a single source of truth, reusing the admin's existing correct ledger-merge logic rather than inventing a new system. **Implemented and tested this session**: Phase 0 (extracted `PaymentInvoicesPanel`'s merge/dedup logic out of `AdminClientWorkspace.js` into a new shared `frontend/src/helpers/paymentLedger.js`, zero behavior change, verified by re-reading the panel); Phase 1 (new customer-scoped `GET /api/my-payment-workspace` endpoint, `backend/controller/user/getMyPaymentWorkspace.js`, adapted from the admin-only `getAdminUserWorkspace.js`, tested against the live DB with real generated test JWTs — correct per-customer scoping confirmed, no cross-customer leakage); Phase 2 (`getWalletHistory.js` query narrowed with a `$or` filter so Wallet page only shows true wallet-balance transactions — live-data check found zero `deposit`/`refund`-type transactions exist yet in production, so wallet history is now correctly empty until a real recharge happens, confirmed with the user before proceeding); Phase 3 (new `frontend/src/components/PaymentStatusChip.js` on `OrderPage.js`, derives Paid/Partial/Pending/Overdue purely from the order object `OrderPage.js` already fetches — no new API call — verified against 10 real sampled orders' actual field values). **Phase 5 (a new global cross-customer admin page) was explicitly rejected by the user** — the existing per-customer Admin → Client Detail → Payment & Invoices view stays the only admin view. **Phase 4 (retire `UserInvoices.js`, add an Orders-page side panel with installment/invoice detail + Pay Now) is approved in concept but NOT implemented** — mid-session discovery that `InstallmentPayment.js` is a full standalone routed page, not an embeddable widget, means the panel's "Pay Now" approach (navigate to existing page vs. rewrite the QR/wallet flow inline) is still an open decision the user must make before any Phase 4 code is written. Read this before touching `AdminClientWorkspace.js`, `OrderPage.js`, `WalletDetails.js`, `getWalletHistory.js`, or before starting Phase 4.

### `24_ORDER_DETAIL_INVOICE_UI_REWORK.md`
UI-only rework (no backend changes) of the customer `/order-detail/:orderId` page (`OrderDetailPage.js`) across multiple user-corrected rounds: removed the status badge, Download Invoice button, Track Project button, and Order Progress tracker; removed the Payment Summary card entirely; simplified `Plan Snapshot` to Start Date/End Date/Payment Due Date/Payment Cycle (no order ID); made the Invoice History card render for **any** order with invoice records regardless of category (was previously, incorrectly, gated to `website_updates` only); restyled invoice/installment rows to match a user-provided real-invoice-row screenshot; added a new `frontend/src/pages/InvoiceDetailPage.js` (route `/invoice-detail/:invoiceId`) for per-invoice detail + Download/Pay Now. Also root-causes (but explicitly does **not** fix, per user instruction) a real backend gap: `monthlyInvoiceModel` documents are only ever created for monthly-recurring-plan orders — one-time-purchase and installment orders never get an invoice record at all, confirmed via a live read-only DB query on a real affected order. A temporary, clearly-commented dummy-invoice fallback (`DUMMY_INVOICES` in both new/changed files) was added so row-styling could keep being iterated on without that backend feature existing yet — **must be removed once real invoices exist for every order type**. Read this before touching `OrderDetailPage.js`, `InvoiceDetailPage.js`, `customerRoutes.js`, or before starting the deferred backend invoice-generation work.

### `25_ORDERS_PLANS_UI_AND_ADMIN_PLAN_LISTING.md`
Sequence of small, individually-approved UI-only changes: `OrderDetailPage.js`/`InvoiceDetailPage.js` width fixed to `max-w-7xl` (site-standard); `OrderPage.js` header rebuilt to match `ProjectsAndPlans.js`'s dark-gradient-banner pattern (eyebrow badge, white heading, `Total: {orders.length}` chip, banner-styled Refresh); `OrderDetailPage.js`'s Back button moved from a plain link above the banner into a `PlanDetails.js`-style pill inside the banner, and the emerald "Plan"/"Order" badge below it was removed. New **admin Plans list** (`AdminPlanProductsPage.js`, structural copy of `AdminProjectProductsPage.js`, backed by a new read-only `GET /api/admin/plan-products` -> `getAdminPlanProducts.js`, filtered to `category: 'website_updates'`) plus a new sidebar entry under Website Management, directly below Projects. New **UI-only** admin `AdminCreatePlanPage.js` ("Add Plan") — form fields scoped to what's actually plan-relevant after live-DB verification that `totalPages`/`packageIncludes`/`perfectFor` are dead weight for `website_updates` products (`ProductDetails.js`'s `shouldShowSection()` hides two of them for plans outright); submit only calls `preventDefault()`, no save wiring exists yet. Customer-side: `StartNewProject.js` gained a "Plans" tab (`website_updates` no longer excluded) and had its "Who is it for?" list column removed — **an earlier over-broad attempt that changed both in one pass was explicitly rejected by the user and fully reverted**, then re-done as two separately-approved steps; read Section 6 before repeating that mistake. `ProjectDetailView.js` (the shared customer/admin project detail component) had its section order changed from Description/What You Get/Features/Who Is This For to Description/Who Is This For/What You Get/Features — pure reorder, no markup or data changes. Read this before touching `OrderPage.js`, `OrderDetailPage.js`, `InvoiceDetailPage.js`, `AdminPlanProductsPage.js`, `AdminCreatePlanPage.js`, `AdminLayout.js`'s sidebar config, `StartNewProject.js`, or `ProjectDetailView.js`.

### `plansystem.md`
Requirement-gathering (Sections 1-4, no code) plus two full design proposals (Sections 5-6, design only) for a service plan system: verifies the legacy 3-hardcoded-plan-type system, records the confirmed requirement (admin-selectable service type, generic per-cycle entitlement, portal file-upload limit as a plan benefit, admin runtime edit power), then proposes the core schema/enforcement/cycle-engine design (Section 5) and a full recurring-billing addendum (Section 6, a brand-new invoice/cron/payment system separate from the legacy one). Also documents a live read-only DB check that found one real customer on the legacy system, which is why an additive coexist-then-migrate-later approach was chosen over deleting the legacy system outright. Only a small part of Section 5 (schema fields + admin create endpoint) has actually been implemented — see `26_SERVICE_PLAN_SYSTEM_PHASE_1.md` for what's real. Read this before starting any further plan-system work.

### `26_SERVICE_PLAN_SYSTEM_PHASE_1.md`
Phase 1 implementation of the service plan system designed in `plansystem.md`: a full redesign of the admin "Add Plan" form (`AdminCreatePlanPage.js`) from the legacy 3-plan-type shape into a generic, service-agnostic plan builder (Plan Type dropdown, Portal Access + Limit Scope [per day/week/month/quarter/6-month/year/plan/unlimited/manual] + Files Limit under "Plan Power", Validity Unit/Value + dynamically-filtered Billing Cycle under "Plan Validity"), now wired to a real, new, additive backend save path (`POST /api/admin/plans/create` -> new `createServicePlan.js`, explicit field whitelist, full server-side re-validation, never touches `uploadPoduct.js`). New additive `isServicePlan`/`servicePlan` fields on `productModel.js`; `getAdminPlanProducts.js`'s listing query widened so new plans appear in the existing admin Plans list alongside legacy ones with no visual distinction. Customer-facing purchase/enforcement/billing is explicitly out of scope for this phase — read `plansystem.md` Sections 5-6 before starting that work. Read this before touching `AdminCreatePlanPage.js`, `AdminPlanProductsPage.js`, `productModel.js`'s `servicePlan` fields, or `getAdminPlanProducts.js`.

### `27_SERVICE_PLAN_RENAME_AND_LEGACY_MIGRATION.md`
Full "Generic Plan" -> "Service Plan" rename across the entire codebase (schema fields, files, docs — `isGenericPlan`/`genericPlan` -> `isServicePlan`/`servicePlan`, `createGenericPlan.js` -> `createServicePlan.js`, `26_GENERIC_PLAN_SYSTEM_PHASE_1.md` -> `26_SERVICE_PLAN_SYSTEM_PHASE_1.md`); made Service Plan products visible to customers for the first time (`StartNewProject.js`'s "Plans" tab renamed "Service Plans" and widened to include `category: "service_plan"`); new pre-purchase preview page `ServicePlanDetail.js` (route `/service-plan-detail/:planId`, fetches via the existing `POST /api/product-details`, dummy "Proceed to Payment" button by explicit user instruction); confirmed via route-file inspection that admin can no longer create a legacy-type plan through any UI path (`UploadProduct.js`/`AllProducts.js` are unrouted dead code); new additive Service Plan tracking fields on `orderProductModel.js` (`isServicePlan`, `servicePlanSnapshot`, cycle-tracking fields, `servicePlanStatus`); a dry-run-first, backup-first, additive-only migration script (`backend/scripts/migrateLegacyPlansToServicePlan.js`) that gave the one real live customer's 3 legacy plan orders new Service Plan fields without touching any legacy field. Explicitly **not done**: no enforcement/cycle engine reads the new fields yet (the customer's plan is still entirely driven by legacy code), no purchase button was wired, the 5 legacy product templates were not migrated, and no legacy code was removed. Read this before touching `orderProductModel.js`'s new `servicePlan*` fields, `StartNewProject.js`, `ServicePlanDetail.js`, `customerRoutes.js`, or before running/adapting the migration script again.

### `28_CART_SYSTEM_AND_ADD_MORE_PAGES.md`
Built the customer-facing Cart from scratch: a rejected e-commerce-style multi-item basket design (`CartContext.js`/`CartPage.js`/`CartAddedDrawer.js`, deleted, not kept as backups) superseded by a single always-available slide-in drawer (`DraftOrderSavedDrawer.js`, backed by `DraftOrdersContext.js`) with no full-page route at all (`DraftOrdersPage.js` was built then deleted once confirmed redundant — the drawer alone covers add/remove/edit-add-ons/select/pay). Wires `ProjectDetailView.js`'s "Add to Cart" and `ServicePlanDetail.js`'s "Proceed to Payment" (both previously dummy stubs) into this drawer. Combined (not per-item) checkout: a checkbox-based `selectedForCheckout` list drives one running total and one "Pay Now" button — still a dummy stub, the real multi-order all-or-nothing backend endpoint was explicitly deferred. New persistent, periodically-`wiggle`-animated `FloatingCartButton.js` (bottom-right, new Tailwind `wiggle` keyframes). Rebuilt "Add New Page" as a proper `+`/`-` quantity control (matching `createOrder.js`'s pre-existing `additionalQuantity` expectations) living inside "Add More to Your Project" instead of a checkbox in the generic features grid; root-caused and fixed one live data gap (College Website's `productModel.additionalFeatures` was missing two compatible features) via a dry-run/backup-first script. Also added inline page-count/USP display on the list and detail pages. Nothing here touches the old public-site `Cart.js`/`cartProduct` system, which remains completely separate and untouched. Read this before touching any of `DraftOrdersContext.js`, `DraftOrderCard.js`, `DraftOrderSavedDrawer.js`, `FloatingCartButton.js`, `ProjectDetailView.js`'s page-quantity logic, or before building the real cart-persistence/payment backend.

### `29_STARTPROJECT_INTAKE_PAGE_AND_PORTAL_GLASSMORPHISM.md`
Two-part session. **(1)** New parallel entry page `frontend/src/pages/startproject.js` now live at the existing `/start-new-project` route (swapped in `customerRoutes.js`; the old `StartNewProject.js` catalog page is untouched and restorable in one line) — 6 intent-based cards (New Website Project / New Software Project / Add New Feature / Maintenance & Bug Fix / Content Update / Design & UI Changes), each navigating to a **not-yet-built** placeholder route `/start-new-project/build/:categoryId`. This is Step 1 only of a much larger planned pivot away from the pre-created product/plan catalog toward a fully custom, admin-reviewed, conversational multi-step requirement-gathering flow — the form itself, the no-payment request path, and catalog deletion are all explicitly deferred, not started. **(2)** A user-supplied real background image (`frontend/assets → frontend/src/assets/BG.png`) plus an established 3-tier glass design system (dark-glass cards for headings/actions, light-glass with **black text** for dense data tables, green "action-pill" buttons) were rolled out across nearly the entire customer portal: `DashboardLayout.js` (sidebar + shared main), `SharedHeader.js` (site-wide dark theme), and 14+ individual pages (`CustomerDashboard.js`, `ProjectsAndPlans.js`, `OrderPage.js`, `WalletDetails.js`, `frontend/src/chess/GamesListPage.js`, `Profile.js`, `ContactSupport.js`, `frontend/src/components/TicketsList.js`, `frontend/src/components/CustomerWorkspaceTabs.js`, and others). `Profile.js`/`ContactSupport.js`/`GamesListPage.js` were further restructured to a fully open, wrapper-less layout matching `startproject.js`'s exact top-level shape. Payment-input surfaces (Wallet recharge modal) were explicitly kept solid white, not glassed. Read this before touching any customer-portal page's background/visual styling, before building the Step 1 multi-step form, or before touching `DashboardLayout.js`/`SharedHeader.js`/`CustomerWorkspaceTabs.js`.

### `30_PROJECTS_AND_PLANS_UI_CONSISTENCY_REWORK.md`
`ProjectsAndPlans.js` reworked (UI-only) from its old single-seamless-card layout (banner rounded-top + table rounded-bottom, joined) into an open-layout heading (no bounding card, matching `startproject.js`/`GamesListPage.js`/`ContactSupport.js`) followed by one `TicketsList.js`-style dark-glass card containing a title+filter+counters header row (all wrapped/centered in one row, no extra height) and a table with a `bg-white/5` header band beneath it. `CustomerWorkspaceTabs.js` gained a new backward-compatible `variant="inline"` prop (default unchanged, used only by `ProjectsAndPlans.js`) and a scrollbar-hiding fix applied to all 4 pages that use it. The same session then applied the identical pattern to `CustomerDashboard.js`: open heading (renamed "What is active now" → "Dashboard"), duplicate mini-stat cards removed, the 4 `MetricCard`s ungrouped into an open grid, the table converted to the same `TicketsList.js`-style card, and the "Live project" `MetricCard` made dynamic/clickable (reuses the pre-existing `primaryAction` logic — routes to the active project/plan if one exists, else becomes "Start New Project"). Also fixes a Dashboard-only white-flash-on-load bug (a stale, pre-dark-theme `if (loading)` skeleton block was found and deleted, along with its now-dead `loading` state) and swaps `Wallet`/`Orders` order in `DashboardLayout.js`'s sidebar (`Wallet` now under "More", `Orders` now a primary quick link). Read this before touching `ProjectsAndPlans.js`, `CustomerDashboard.js`, `CustomerWorkspaceTabs.js`, or `DashboardLayout.js`'s sidebar link arrays.

### `31_PROJECT_DETAILS_UI_TEMPLATE.md`
`ProjectDetails.js` (customer-side only; admin view fully untouched) reworked into the confirmed, user-declared **site-wide detail-page template**: open centered header (Back button `absolute left-0`, large size `px-5 py-3 text-lg`, heading truly centered on the row, not offset by the button), no page-level bounding wrapper card (banner and content both float directly on the `BG.png` background), the 3-column progress/timeline/checkpoint layout merged into one dark-glass card (`bg-white/10`, matching `ProjectsAndPlans.js`'s list-card tone exactly — not the lighter `bg-white/55` variant, which was tried and rejected), all blue removed and replaced per-context (emerald for progress/completion/real buttons, neutral white/slate for in-progress/active/selected, amber for pending-info, red untouched for errors), a stray timeline connector-line deleted, and a real "Request Update" action button corrected from glass/badge-style back to solid emerald. The same session then applied the identical template to `PlanDetails.js` (donut+Snapshot merged into one card, Update-History/Request-Details as `border-r`-divided sections of one single card, `RequestHistoryItem` dark-glassed, all blue removed) and fixed `OrderPage.js`'s `OrderStatusBadge` "Completed" state, whose text color+weight were a light-glass leftover invisible against the now-dark rows — matched byte-for-byte to the timeline's completed-checkpoint badge styling. A recurring "extra `</div>` after wrapper removal" syntax bug on both `ProjectDetails.js` and `PlanDetails.js` was resolved both times with the same `@babel/core`-based verification script (see the doc's Section 2). Read this before touching `ProjectDetails.js`, `PlanDetails.js`, `OrderPage.js`'s `OrderStatusBadge`, and **before starting the same rework on any other detail page** (`OrderDetailPage.js`, `InvoiceDetailPage.js`, `ServicePlanDetail.js`, etc.) — this is now the required pattern for all of them, not just these two files.

### `32_START_NEW_WEBSITE_QUESTION_FLOW_AND_DASHBOARD_HIGHLIGHT.md`
Builds Step 2 of the pivot planned in `29_...md`: a new Typeform-style branching question flow, `frontend/src/pages/StartNewWebsiteBuild.js` (route `start-new-project/build/new_website`) — Budget -> (5k-30k branch: Ownership -> Customize-or-Contact-us) or (30k+ branch: Website Type, 5 cards) -> a final in-place confirmation screen with an editable "Your Details" (Name/Phone editable, Email read-only, Redux-prefilled, local-state-only) card and a dummy "Submit Request" stub — plus a placeholder `StartNewWebsiteCustomize.js` (route `.../customize`) for the not-yet-built customize path. Also reduces `startproject.js` from 6 cards to 3 (New Website Project / New Software Project / Features & Updates) and adds a dark-glass "Coming Soon" modal for the two still-unbuilt cards, adds a `highlight` prop to `CustomerDashboard.js`'s first `MetricCard` (solid emerald fill when the card is a real "Start New Project" CTA, neutral when it's showing live-project status). Section 6 records a 3-layer root-cause analysis of a plan-vs-project routing bug and Section 7 **implements the fix** (same session): new shared `frontend/src/helpers/orderType.js` (`isPlanItem`/`isProjectItem`, now recognizing both `website_updates` and `service_plan`), 4 files' duplicate copies removed in favor of the shared helper, `CustomerDashboard.js`'s `getItemLink` made type-aware, and both `PlanDetails.js`/`ProjectDetails.js` gained a post-fetch type-verification redirect (`{ replace: true }`) so any entry point — known or future — lands on the correct detail page. Read this before touching any of those files, `startproject.js`, `CustomerDashboard.js`, or before starting the real customize flow or the real submit-request backend/admin-inbox (both still not built).

### `33_ADMIN_CREATE_PROJECT_FOR_CLIENT.md`
New admin capability: from a specific client's workspace (`AdminClientWorkspace.js` → Projects tab → "Create Project for Client"), admin opens a modal (list stays visible underneath) with a lean project-creation form — name, starting node title, category, total pages, **Price (Base) + Selling Price** (both kept for invoice/discount math), Additional Features/Upgrades (same dropdown-with-tags pattern as `AdminCreateProjectPage.js`, now with a live features-total for invoicing), and a same-submit Payment Type toggle (One-time/Partial, no coupon, no separate payment page). On submit, a new admin-only backend controller (`backend/controller/order/adminCreateProjectOrder.js`, route `POST /api/admin/clients/:customerId/create-project`) creates a small catalog-hidden product (`isHidden: true` + new `isCustomClientProject: true` flag on `productModel.js` — the only schema change this session) and an order pointing at it, immediately `approved`/`in_progress` (no pending-approval wait), and is the **first-ever caller** of the previously-unused `initializeProjectTimeline` (`projectNodeService.js`) — starting node activates immediately. Deliberately built with **zero dependency on the customer-facing storefront** (`createOrder.js`, `DirectPayment.js` not reused/imported) since the public site is planned for future removal. Client sees the new project immediately in their own Orders/Projects-and-Plans list with no changes needed to those files (schema-driven visibility, verified not assumed). Editing an existing client-created project and a separate client-side self-service version of this feature are both explicitly deferred, not built. Read this before touching `AdminClientWorkspace.js`'s Projects tab/`CreateProjectForClientForm`/`CompactWorkspaceCard`'s `headerAction` prop, `adminCreateProjectOrder.js`, or `productModel.js`'s `isCustomClientProject` field.

### `34_DETAIL_PAGE_GLASS_ROLLOUT_CHESS_REWORK_AND_MOBILE_BOTTOM_NAV.md`
Continues the `31_...md` glass template onto four more pages (`OrderDetailPage.js`, `InvoiceDetailPage.js`, `UserInvoices.js`, `TicketDetail.js` — open header, dark-glass cards, zero blue, blue/purple statuses replaced with emerald/amber/red/neutral glass tones). Fixes a full-page-reload bug in `TicketsList.js` (its "View" link was a plain `<a href>`, not a router `Link`; now the whole row is `onClick`-navigable via `useNavigate`, matching `ProjectsAndPlans.js`'s row-click pattern). Chess (`frontend/src/chess/*`) — previously the only feature with no `DashboardLayout` sidebar and no glass styling — was converted to the standard glass system, wrapped in `DashboardLayout`, widened to `max-w-7xl`, and `ChessLobby.js` was rebuilt from a static generic 3-card form into a `StartNewWebsiteBuild.js`-style branching step-flow (mode select → Create's color+palette 2-step sub-flow in a 3-column grid, or Join's single code-input card, or Random's single Find-Opponent card). Also fixes a real backend identity bug: `chessRoomManager.js`'s `getPlayerColor()` was stringifying populated Mongoose sub-documents (`{_id,name,email}`) instead of their `_id`, silently breaking color/opponent detection in the "Resume a game" list once `players.white`/`black` were populated for name/email display — fixed at the helper level, not just the one call site; `chessSocket.js`'s `roomStatePayload()` is now async and returns populated `{name,email}` per player. New `frontend/src/components/MobileBottomNav.js` — a 5-tab fixed bottom bar (Dashboard/Projects/Start/Games/More) for `<lg` viewports, wired into `DashboardLayout.js` using its existing `quickLinks`/`secondaryLinks` active-path logic; "More" opens the pre-existing `MobileSidebarDrawer` rather than duplicating it. Read this before touching any of the four glass-converted pages, `TicketsList.js`, anything under `chess/`, or `DashboardLayout.js`'s nav-link arrays.

### `35_CATEGORY_BASE_PRICE_AND_PROJECT_FEATURES_SYSTEM.md`
Reworks the admin "Create Project for Client" form's pricing: a new admin-only `Category Base Price` collection/page (`categoryBasePriceModel.js`, page at `/admin-panel/project-setup/base-price`, new sidebar section `Project Setup` separate from `Website Management`). Additional Features are **not** a new collection — after a live DB check found 8 real, already-in-use `feature_upgrades` products (Live Chat, Payment Gateway, etc.), a first attempt at a separate `projectFeatureModel` system was built then explicitly removed in the same session; features continue to come from the existing `feature_upgrades`-category `productModel` products, unchanged. `CreateProjectForClientForm` (`AdminClientWorkspace.js`) lost its Project Name field (`serviceName` now derived server-side from category), Price (Base) became a read-only auto-fetched field, a new Reference Total (Base + Features) display line was added, and Discount now compares Selling Price against Reference Total instead of the old typed price. `adminCreateProjectOrder.js` no longer trusts client-submitted price/feature-price data — it re-derives base price from `categoryBasePriceModel` and feature price from `productModel` (re-filtered to `feature_upgrades`) server-side. `productModel.js` gained an additive `clientProjectFeatures` snapshot field (name+price per selected feature, `ref: 'product'`) separate from the pre-existing `additionalFeatures` field, which remains untouched. Read this before touching `CreateProjectForClientForm`, `adminCreateProjectOrder.js`, `categoryBasePriceModel.js`, `productModel.js`'s `clientProjectFeatures` field, or `AdminLayout.js`'s sidebar config.

### `36_ADMIN_FEATURE_PRODUCTS_MANAGEMENT_PAGE.md`
Fills a gap left by `35_...md`: admin had no page to list/create/edit/delete `feature_upgrades`-category products (Live Chat, Payment Gateway, etc.) — `AdminProjectProductsPage`'s backend explicitly excludes this category, and no other routed page covered it. New `frontend/src/pages/AdminFeatureProductsPage.js` (route `/admin-panel/project-setup/features`, second entry in the `Project Setup` sidebar section alongside Category Base Price) — a list page + modal Add/Edit form, reusing the pre-existing generic, category-agnostic `uploadProduct`/`updateProduct`/`deleteProduct` endpoints unchanged, backed by one new list-only endpoint `GET /api/admin/feature-products` (`getAdminFeatureProducts.js`). Price is a single field (not price+sellingPrice — a feature has no discount concept, both are saved equal). A live DB check found most real features already carry `packageIncludes`/`keyBenefits`/`compatibleWith`/`formattedDescriptions` data that no page exposed — all four were added to the form (Description via the existing `RichTextEditor`, Key Benefits via the existing `keyBenefitsOptions` react-select list, Package Includes as free-text tags, Compatible With as a small fixed category list). Editing here affects both the customer-facing storefront and the admin "Create Project for Client" form's feature dropdown, since both read the same `productModel` documents — expected, not a side effect. Same session: user compared this whole flow against the catalog "Add Project" form, confirmed the category-base-price + feature-price + dynamic Reference Total + manual Selling Price design is the intended final shape, and asked for a per-category **Description** to match — `categoryBasePriceModel.js` gained a new `description` field, and `CreateProjectForClientForm` shows the selected category's description as a read-only reference box (display-only, not submitted with the order). Both `AdminFeatureProductsPage.js` and `AdminCategoryBasePricePage.js` were then reworked twice more: first to a shared list+modal (click row → edit form) pattern, then — per explicit user request to match `AdminProjectProductsPage.js`'s existing list-shell exactly — to a stub pattern: an "Add Feature"/"Add Category" header button and every row click both just show a "next step" toast, no working create/edit/delete. **The real CRUD code (modal forms, `uploadProduct`/`updateProduct`/`deleteProduct` calls) was removed from both page files in this final rework** — the backend endpoints themselves are untouched and still work, just currently uncalled by these two pages. Read `36_...md` Section 4d before assuming either page's Add/Edit works. Read this before touching `AdminFeatureProductsPage.js`, `getAdminFeatureProducts.js`, `AdminCategoryBasePricePage.js`, `categoryBasePriceModel.js`, or `AdminLayout.js`'s `Project Setup` sidebar section.

### `37_NEW_INVOICE_SYSTEM_FOR_ADMIN_CREATED_PROJECTS.md`
Full-trace investigation confirmed two pre-existing, unrelated invoice-shaped systems: one-time/installment project purchases had no formal invoice anywhere (`order.installments[]` is payment-tracking only, `OrderDetailPage.js`/`InvoiceDetailPage.js` fall back to hardcoded `DUMMY_INVOICES` for every such order); recurring/yearly-billed-monthly plans have a real `monthlyInvoiceModel` but only via a narrow daily cron, and `UserInvoices.js`/`/my-invoices` is a confirmed-dead route. New, generic `backend/models/invoiceModel.js` (`invoiceType: 'project'|'plan_renewal'`, `lineItems[]`, no fake recurring-only fields) built rather than retrofitting `monthlyInvoiceModel`'s required recurring fields — explicit user decision since the old system is planned for future removal. Wired **only** into `adminCreateProjectOrder.js` (creates one invoice per installment, or one for full payment, itemized with base price + selected features); `monthlyInvoiceModel`/`autoRenewalCron.js`/the customer storefront (`createOrder.js`) are all untouched this session. `getMyPaymentWorkspace.js`/`getAdminUserWorkspace.js` widened to merge the new invoices into their existing `invoices` array — `OrderDetailPage.js`, `InvoiceDetailPage.js`, and the admin Payment & Invoices ledger (`paymentLedger.js`) all pick up real data automatically with **zero frontend changes**, confirmed by reading their consumption code before deciding this. Known gap, explicitly deferred: no "mark paid" UI exists yet for the new model — admin-created project invoices stay `unpaid` until a future session wires that. Read this before touching `invoiceModel.js`, `generateInvoiceNumber.js`, `adminCreateProjectOrder.js`, `getMyPaymentWorkspace.js`, or `getAdminUserWorkspace.js`.

### `38_TWO_STEP_PAYMENT_SETTINGS_AND_PAYMENT_PENDING_LOCK.md`
`CreateProjectForClientForm` converted to an interactive 2-step flow (Project Details → "Next: Payment Settings" → Payment Settings step → "Create Project", one form/one submit, no popup) — Step 2 shows the amount due now (full amount or first installment) and lets admin either "Record Payment Now" (Payment Method/Transaction Reference/Note, visual pattern copied from the existing `AdminPaymentRecordDetail.js` "Record Payment" panel, not imported since its backend is recurring-plan-specific) or "Just Add Project, Let Client Pay the Bill". New `markProjectInvoicePaid()` helper inside `adminCreateProjectOrder.js` marks the first invoice (`37_...md`) paid and creates a matching `transactionModel` record — deliberately not reusing `invoiceLifecycle.js`'s `markInvoicePaidAndResumePlan()`, which is hardcoded to `monthlyInvoiceModel` and runs plan-resume logic that doesn't apply to one-time projects. `getOrderDetails.js` widened with `hasUnpaidInvoice`/`unpaidInvoice` fields (queries the new `invoiceModel`, no new endpoint); `ProjectDetails.js` (customer-side) shows a new amber "Payment Pending" banner and disables the "Request Update" button when the project's invoice is still unpaid — the client can still see/track the project, only interactive actions are gated, per explicit user scoping. Read this before touching `CreateProjectForClientForm`, `adminCreateProjectOrder.js`, `getOrderDetails.js`, or `ProjectDetails.js`'s payment-gating logic.

### `39_PROJECT_NODE_SYSTEM_PHASE_2_3_DONE_PHASE_4_PENDING.md`
**Current source of truth for the project-node system — supersedes `admin-nodes.md`'s status.** Wires the previously backend-only dynamic node system (`projectNodeService.js`/`projectNodeController.js`, built earlier but never called from any frontend) end to end. Phase 2: `AdminClientWorkspace.js`'s project-detail node UI and `AdminProjectCheckpointDetail.js` switched from `item.checkpoints`/local-state-only preview buttons to `item.projectNodes` and 5 new real `SummaryApi` node endpoints (Add/Delete/Restore/Visibility/Reset), each with `toast` feedback and an auto-refetch on success; the non-functional "Show old history to client" checkbox was removed rather than left as a silent no-op (backend hardcodes `showToClient: false` regardless of input); "Send Update" (no backend equivalent) was removed, leaving only "Add Node"/"Add Node & Send". Phase 3: `createOrder.js` (the regular customer-purchase path) now calls `initializeProjectTimeline()` instead of hardcoding legacy `checkpoints`, using each category's `categoryBasePriceModel.startingNodeTitle` with a `"Project Started"` fallback so checkout never hard-fails; a real bug was found and fixed in `orderProductModel.js`'s legacy `pre('save')` checkpoint-copy hook, which didn't check `projectTimelineVersion` and would have silently re-populated legacy checkpoints on every new order otherwise. `ProjectDetails.js` (customer-facing) fully migrated off `order.checkpoints` onto `order.projectNodes` — the old "next upcoming checkpoint" grayed-out placeholder concept was removed entirely (no predefined future list exists in the new system) and an internal-ID "Timeline: `<key>`" stat tile was removed from the detail panel. `order.projectProgress` itself was untouched (shared field, written by both systems, gated by `projectTimelineVersion`) so the 40%/75% installment-unlock thresholds keep working unmodified. **Update (later session):** the central Phase 4 decision was made — migrate, not leave-alone (justified by the then-current low client count, under 10) — and executed via `backend/scripts/migratePreExistingOrdersToNodeSystem.js` (dry-run verified, backup-first, then applied): all 9 pre-migration `isWebsiteProject: true` orders now carry real per-checkpoint-derived `projectNodes` history and read `projectTimelineVersion: 1`, fully live on the node system end to end. `AppConvertingBanner.js` was also fixed the same session to count `projectNodes` instead of reading `order.checkpoints` directly. The 4 non-website legacy orders were intentionally left unmigrated (node system only supports `isWebsiteProject: true`). **Also done (same later session):** the legacy `checkpoints` schema/hooks were removed from `orderProductModel.js`/`productModel.js` (backup-first, re-verified against the live DB afterward). **Still not done:** the confirmed-unreachable dead-code cleanup (`ProjectWorkspaceModal.js`, unused `emailService.js` exports, unrouted `UploadProduct.js`/`AdminEditProduct.js`/`AllProducts.js`/`AdminProductCard.js` — all confirmed importable only from unrouted backup folders, not the live app). Read this before touching `AdminClientWorkspace.js`'s project-detail view, `AdminProjectCheckpointDetail.js`, `ProjectDetails.js`, `createOrder.js`, `adminCreateProjectOrder.js`, `projectNodeService.js`/`projectNodeController.js`, `categoryBasePriceModel.js`, or before finishing the remaining Phase 4 dead-code cleanup.

### `40_NODE_EDIT_DYNAMIC_TABS_PAYMENT_LINK_AND_LIST_SSOT.md`
Four independent, individually-approved changes. **(1) Admin client-detail dynamic tabs**: `AdminClientWorkspace.js`'s static `tabs` array became a `useMemo`-derived list — if the client has an active project the `Projects` tab comes first, else an active plan puts `Plans` first, else `Overview` first (original order); `Payment & Invoices` always last. `activeTab` starts `null` and a single `useEffect` (deps `[dataLoading]`) auto-selects `tabs[0]` once after load, then the admin's click always wins. Follows `CustomerDashboard.js`'s derived `primaryAction` pattern; an earlier patch-shaped attempt (extra `hasAutoSelectedTab` state + `resolvedActiveTab` fallback var) was reverted first. **(2) Real in-place node edit**: the earlier UI-only delete+create "correction" idea could not edit a 0% starting node (`appendProjectNode()` rejects anything below `current+0.1`), so a proper backend `editProjectNode()` was built in `projectNodeService.js` — works for **every** node: the run's starting node is title-only (locked 0%), every other node's new progress must sit strictly between its neighbours (`prev+0.1` … `next-0.1`) so cumulative order never breaks; writes `editedAt`/`editedBy` + a new `node_edited` audit event, re-syncs `projectProgress`. Additive schema (`editedAt`/`editedBy` on `projectNodeSchema`, `node_edited` in the event enum), new `projectNodeController.editProjectNode`, new route `POST /admin/projects/:orderId/nodes/edit`, new `SummaryApi.editProjectNode`. Frontend: an "Edit" button (enabled only for exactly one selected active node) in `AdminClientWorkspace.js`'s node command bar, `handleSaveCorrection` now a single edit call (delete+create removed), `AdminProjectCheckpointDetail.js` gained `editingNode`/`editBounds`/`onSaveCorrection`/`onCancelEdit` (pre-fills, locks 0% starting node, enforces both bounds, "Save Correction"/"Cancel Edit"). **Supersedes `admin-nodes.md` line 185** ("node edit is not allowed"). **(3) Payment link**: `ProjectDetails.js`'s customer amber "Payment Pending" banner (`38_...md`) got a "Proceed for payment" text-link to `/invoice-detail/:invoiceId` (`order.unpaidInvoice._id`); the invoice-detail page already displays the invoice, but its "Pay Now" is still a dead flow (`DirectPayment.js` ignores the `invoicePayment` state) — Phase B online-pay for the new `invoiceModel` remains unbuilt. **(4) Project/plan list full SSOT**: the recent list (`CustomerDashboard.js`) and full list (`ProjectsAndPlans.js`) had duplicated logic **and** row markup that had drifted apart; unified into shared logic (`helpers/orderType.js` gained `isFinishedItem`/`sortItemsLatestFirst`; new `helpers/orderPresentation.js` owns `getItemStatusMeta`/`getItemSummary`/`getItemTypeLabel`/`getItemTypeAccent`/`getRemainingDays`) and a shared layout (`components/OrderListRow.js` exporting `OrderListRow` + `OrderListHeader`). The **ProjectsAndPlans layout/status version is canonical** (user choice), so the dashboard row changed to match (lost its assigned-developer line + extra progress column, gained emerald=project/amber=plan accents, detailed "Booked"/"Developer Assigned"/"45% Complete" statuses). Sorting is now rank-based (finished items last, latest-update first) everywhere. Not shared: each page's header/tabs/filters/empty-state and its navigate handler. Read this before touching `AdminClientWorkspace.js`'s tabs or `WorkspaceDetailSubpage` node UI, `AdminProjectCheckpointDetail.js`, `projectNodeService.js`/`projectNodeController.js`, `ProjectDetails.js`'s Payment Pending banner, `CustomerDashboard.js`/`ProjectsAndPlans.js`'s list, `helpers/orderPresentation.js`, or `components/OrderListRow.js`.

### `41_CUSTOMIZE_PROJECT_PAGE_AND_FULLPAGE_GLASS_SYSTEM.md`
Three UI-only, individually-approved changes. **(1)** The three remaining customer-portal empty-state CTAs still pointing at the old public site (`CustomerDashboard.js` "Start New Project", `ProjectsAndPlans.js` "Start New Project", `OrderPage.js` "Browse Services") were re-routed from `/home` to the portal's `/start-new-project`; branding/nav/logout/root-redirect `/home` references in `SharedHeader.js`/`RoleBasedHome.js` were left untouched by design. **(2)** `StartNewWebsiteBuild.js`'s customize navigation now derives the primary project category from the flow's `ownership` answer (`self_managed` ⇒ `dynamic_websites`, `we_maintain` ⇒ `standard_websites`) and passes it into `location.state` — only that one navigate block changed. **(3)** `StartNewWebsiteCustomize.js` was rebuilt from a "coming soon" placeholder into a real, form-style **Customize Your Project** page (**UI + read-only DB fetch only, no backend wiring**): four editable glass dropdowns (Primary project / Budget / Content management / Payment), a **capabilities multi-select** populated from `GET /api/get-product` filtered to `category === 'feature_upgrades'` + `!isHidden` + `compatibleWith.includes(projectCategory)` (**no per-feature prices shown**), live feature-refresh + selection-clear when the primary project changes, an estimate total with a permanent "not the final price" disclaimer, and a coupon input (stub). Custom in-file glass dropdowns were used instead of `react-select` (which fights the dark theme). **Website pages** use the existing `Add New Page` product as an always-on `−/+` counter (min 4, max 99, `sellingPrice × pageCount` in the estimate; appears only for Static Website since that product's `compatibleWith` is `['standard_websites']`). Submit uses **Option B** (logically-split, user-confirmed): Full/Partial opens a **UI-only glass payment-confirm popup**, Decide-later shows a success modal directly (no payment). A payment-system audit first confirmed a working engine already exists (`DirectPayment.js` + `InstallmentPayment.js`, product-based via `createOrder`), but it needs a real `product._id` the custom flow doesn't have yet — so the popup is UI-only with a `buildPaymentData()` helper that shapes the selection to the existing `createOrder`/`DirectPayment` contract for one-step wiring later. Its final layout is now the user-approved **full-page glass "documentation" layout system** (Section 4 of the doc) — an open centered header + a single frameless `bg-white/[0.06] backdrop-blur-2xl` sheet with **no bounding box and no `overflow-hidden`** (so open dropdowns aren't clipped), a responsive `lg:grid-cols-[minmax(0,1fr)_360px]` documentation grid, and `border-t`/`border-l`-divided `<section>`s with emerald eyebrow labels. **This is the new default layout for customer-portal full-page form/detail pages, especially any with open dropdowns/popovers.** Read this before touching `StartNewWebsiteCustomize.js`, `StartNewWebsiteBuild.js`'s customize navigation, the three empty-state CTAs, or before building a new full-page form/detail page.

### `42_CUSTOMIZE_FLOW_BACKEND_WIRING_AND_INPAGE_PAYMENT.md`
Wires the previously UI-only Customize page (`41_...md`) to a real backend up to proceed-to-payment, **approval-based** (matches the existing customer flow). Audit-first finding: the customize flow is **product-less** (no `product._id`, no base price on the request), so `createOrder.js` (`findById(productId)`) and `DirectPayment.js` (needs `product._id`, and **always creates a new order** → would duplicate) can't be reused directly; `InstallmentPayment.js` (pays an already-created order by `orderId` via `/wallet/verify-payment`, no second order) is the right pattern, and `adminCreateProjectOrder.js` is the exact product-less create blueprint. **New backend controller `customerCreateCustomProjectOrder.js`** (route `POST /api/customer/custom-project-order`, `authToken`): auth via `req.userId` (no admin guard), **re-derives price server-side** (`categoryBasePriceModel` base + `feature_upgrades` compatible with the category; `Add New Page` priced `sellingPrice × clampedPageCount`, clamp 4…99), builds a **hidden product** (`isCustomClientProject`), creates the order **`pending-approval`** (vs the admin path's `approved`), partial → `buildInstallments` (2 ⇒ 50/50, 3 ⇒ 30/30/40, byte-aligned with the admin one and the frontend `INSTALLMENT_OPTIONS`), `initializeProjectTimeline()` 0% node; returns `{ orderId, finalPrice, installments, firstInstallmentAmount }`. **Frontend `StartNewWebsiteCustomize.js`**: new partial installment chooser (2-vs-3 + estimate breakdown), `handleSubmit` wired for all three paths (decide-later = order only → success; full/partial = create order → payment popup, with a duplicate-order guard that reuses `createdOrder` on reopen and a `useEffect` that clears it when the selection changes), and an **in-page wallet/UPI-QR payment step** (reuses `InstallmentPayment.js`: wallet `/wallet/deduct` + `/wallet/verify-payment` with `orderId`, or UPI QR → verify — never a second order; partial pays only installment #1). `buildPaymentData()` stub removed; new endpoint `SummaryApi.createCustomProjectOrder`. **Still stubbed/out of scope**: coupon Apply (sent + stored as `couponApplied`, not validated/discounted); no invoice created by the customer endpoint (transaction-based approval via `/wallet/verify-payment`, consistent with `createOrder.js`); the on-page estimate stays client-side/display-only (backend `finalPrice` is authoritative). Read this before touching `customerCreateCustomProjectOrder.js`, the custom-project route, `StartNewWebsiteCustomize.js`'s submit/payment wiring, or `common/index.js`'s `createCustomProjectOrder`.

### `43_LEAD_CRM_SYSTEM_PHASE_1_TO_6A.md`
New admin-side **Lead / CRM system** — a prospect pipeline that lives *before* a person becomes a registered customer. Leads live in a brand-new `leadModel` collection (never `userModel`), following the same "reuse SSOT, same backend/DB, no separate admin backend" precedent as `33_...md`. Built in six approved sub-phases: **Phase 1** (`createLead.js`/`getLeads.js`, `AdminLeadsPage.js` at `/admin-panel/leads` reusing the Clients list-shell + an Add-Lead modal, new `Leads` sidebar entry above `Clients`; **phone required** for every lead, email optional at create); **Phase 2** (`getLeadDetail.js`/`updateLead.js`, `AdminLeadDetailPage.js` at `/admin-panel/leads/:leadId` — contact card, 6-stage pipeline pill selector `New→Contacted→Qualified→Proposal Sent→Won→Lost`, and a follow-up log; status-change and follow-up are **deliberately separate actions**); **Phase 3** (`globalSearch.js` `GET /api/admin/search`, `AdminGlobalSearch.js` mounted in the sidebar top — one debounced box searches **both** `userModel` customers and `leadModel`, badge-tags each result `Client`/`Lead`, converted leads excluded; read-only, owns no data); **Phase 4** (convert: `convertLead.js` creates a `userModel` customer reusing `userSignUp.js`'s hashing with the **universal default password `"1234"`** + new additive `mustResetPassword` flag on `userModel`, links the lead `convertedToUserId`+`Won`+read-only with a "View as Client" link; new `setNewPassword.js` `/api/set-new-password` — the **only** password-change endpoint in the codebase; `userSignIn.js` returns `mustResetPassword`; `postLogin.js` routes a first-login user to the new `SetNewPassword.js` `/set-new-password` page with a soft **"Skip for now"**; email **and** phone required to convert since email is the only login identifier); **Phase 6A** (`uploadProposal.js` `POST /api/admin/leads/:leadId/proposal` — **versioned** `proposals[]` array, reuses the existing `GoogleDriveService` + `multer`, first upload auto-advances status to "Proposal Sent"; `AdminLeadDetailPage.js` gains a Proposals & Quotations card with a version download timeline). **SSOT**: a lead's truth is `leadModel`, a client's is `userModel` (`roles:"customer"`, untouched — `getAdminClients.js` filters by role so leads never leak into Clients); convert is an ownership handoff, one active record owned by one collection. **Not built (explicitly deferred)**: Phase 5 CSV bulk import (paused by user), Phase 6B in-app quotation builder, email/WhatsApp "send" integration (today everything is manual/record-only), an optional create-time "already a client" warning, and a configurable universal password. Read this before touching `leadModel.js`, anything under `backend/controller/lead/`, `setNewPassword.js`, `userModel.js`'s `mustResetPassword`, `userSignIn.js`'s login response, `AdminLeadsPage.js`/`AdminLeadDetailPage.js`/`SetNewPassword.js`/`AdminGlobalSearch.js`, `postLogin.js`'s first-login gate, `AdminLayout.js`'s sidebar/search, or the lead/set-password routes.

## Removed Legacy Docs

`06_CODE_AUDIT_FINDINGS.md`, `07_CUSTOMER_HOMEPAGES_ANALYSIS.md`, `08_HOMEPAGE_CLEANUP_COMPLETE.md`, `09_MVP_CONVERSION_COMPLETE.md`, `10_CUSTOMER_ONLY_LOGIN_SYSTEM.md`, `11_ADMIN_LOGIN_IMPLEMENTATION.md`, and `UPDATES_SUMMARY.md` were deleted in a later doc-cleanup session, to reduce how much stale context an AI has to read through before finding current information. Each was individually read and verified first: all described states that had already been fully superseded (e.g. an admin-panel-removal experiment later reverted, deleted homepage files, orphaned admin-dashboard files that no longer exist in the codebase either way) with nothing describing current live code that isn't already covered in `00_CURRENT_SYSTEM.md` or elsewhere in this index. Full copies are kept at `frontend/src/DOCS/deleted-legacy-docs-backup/` if old context is ever needed. (One of them contained a plaintext admin password — flagged to the user at deletion time, not otherwise handled here.)

## Current High-Level Map

- Public entry: `/` uses `RoleBasedHome`
- Customer home: `/home`
- Login: `/login`
- Customer dashboard route: `/dashboard`
- Customer purchase-history route: `/order`
- Customer "Start New Project" entry route: `/start-new-project` now renders the new `startproject.js` intake-card page (Step 1 of a planned catalog-to-custom-request pivot), not the old `StartNewProject.js` catalog list — see `29_STARTPROJECT_INTAKE_PAGE_AND_PORTAL_GLASSMORPHISM.md`
- Admin dashboard route: `/admin-panel/dashboard`
- Customer header: `CustomerHeader`
- Admin header: `AdminHeader`
- Customer dashboard shell: `DashboardLayout`
- Customer dashboard page: `CustomerDashboard`
- Customer project/plan list page: `ProjectsAndPlans`
- Customer order detail page: `OrderDetailPage`
- Customer purchase-history list page: `OrderPage`
- Admin dashboard page: `AdminDashboard`
- Admin project-product UI route: `/admin-panel/website-management/projects` (`AdminProjectProductsPage`); list UI is active, API wiring is pending
- Admin client list sorting source: existing `GET /api/admin/clients` response field `latestActivityAt`
- Node update status: dynamic node schema/service, admin node UI, and customer-facing node UI are all fully wired end to end for every `isWebsiteProject: true` order — both new orders (customer-purchased and admin-created) and the 9 pre-existing orders migrated in a later session via `backend/scripts/migratePreExistingOrdersToNodeSystem.js`. All of them read `projectTimelineVersion: 1`. The 4 non-website legacy orders remain on legacy `checkpoints` data by design (node system doesn't support their type). Remaining Phase 4 work is legacy schema field/hook removal only (safe now, not yet executed) — no migration decision is open anymore. Read `39_PROJECT_NODE_SYSTEM_PHASE_2_3_DONE_PHASE_4_PENDING.md` (current status) before `admin-nodes.md` (original design doc, mostly superseded) and `13_PROJECT_CREATION_AND_APPROVAL_PLAN.md` before extending the flow
- New project creation direction: admin `Website Management > Projects` now has the Clients-style list UI and an active Add Project form at `/admin-panel/website-management/projects/add`; the form now saves via the existing `POST /api/upload-product` endpoint. Read `13_PROJECT_CREATION_AND_APPROVAL_PLAN.md` before extending it
- `AdminCreateProjectPage.js` now saves the whole project on submit (Cloudinary image upload via the existing `helpers/uploadImage.js`, `productModel.startingNodeTitle` field added to close a real pre-existing schema gap, checkpoints deliberately left server-generated — not duplicated client-side), has a live-data Additional Features multi-select (`GET /api/get-product` filtered to `feature_upgrades`, no category filtering by design), a rebuilt "Who is it for?" field (`PerfectForField.js`: free text, live text-suggestion dropdown, keyboard-navigable, auto-applies known icons from the `perfectForSuggestion` collection, full icon-grid for new text, new suggestions synced to the dictionary on form submit only), and "What's Included" still on the legacy fixed-dropdown `PackageSelect` + `packageOptions.js` pattern (unchanged, separate future task). Read `17_ADD_PROJECT_FORM_AND_PERFECT_FOR_AUDIT.md` before touching this file
- Known unfixed disconnect: `ProductDetails.js`'s customer-facing "Customize Your Plan" section does **not** use a product's `additionalFeatures` selection at all — it independently filters all `feature_upgrades` products by their own `compatibleWith`/category match. A product can have features selected in `AdminCreateProjectPage.js` that never appear to the customer. Documented, not fixed — see `17_ADD_PROJECT_FORM_AND_PERFECT_FOR_AUDIT.md` Section 8 before touching this.
- Full audit handoff: read `14_CODEBASE_AUDIT_INDEX.md` first when resuming work in a new chat or by another AI
- Admin plan-product UI route: `/admin-panel/website-management/plans` (`AdminPlanProductsPage`, read-only list, `GET /api/admin/plan-products`) and `/admin-panel/website-management/plans/add` (`AdminCreatePlanPage`, UI-only form, no save wiring). Sidebar entry sits directly under Projects inside "Website Management". Read `25_ORDERS_PLANS_UI_AND_ADMIN_PLAN_LISTING.md` before extending either.
- Customer-facing Service Plans: `StartNewProject.js`'s "Service Plans" tab (`TAB_CATEGORIES.plans`) now includes both legacy `website_updates` and new `service_plan` category products. Clicking a `service_plan` row opens the new `/service-plan-detail/:planId` (`ServicePlanDetail.js`) preview page; legacy plans still open `/start-new-project/:projectId` unchanged. `ServicePlanDetail.js`'s "Add to Cart" button now adds the plan to the customer Cart drawer (see `28_CART_SYSTEM_AND_ADD_MORE_PAGES.md`) — the drawer's own "Pay Now" is still a dummy stub, no real payment wiring exists yet. Read `27_SERVICE_PLAN_RENAME_AND_LEGACY_MIGRATION.md` and `28_CART_SYSTEM_AND_ADD_MORE_PAGES.md` before touching either file.
- Customer Cart: a single global slide-in drawer (`DraftOrderSavedDrawer.js`, state in `DraftOrdersContext.js`), opened from `SharedHeader.js`'s cart icon or the persistent `FloatingCartButton.js` — **not** a routed page (no `/cart` route exists). In-memory only, cleared on refresh. Both `ProjectDetailView.js`'s "Add to Cart" and `ServicePlanDetail.js`'s "Add to Cart" feed it. "Add New Page" is a quantity stepper inside "Add More to Your Project", not a checkbox. Read `28_CART_SYSTEM_AND_ADD_MORE_PAGES.md` before touching any of this.
- **Admin "Create Project for Client" pricing (current)**: `CreateProjectForClientForm` (`AdminClientWorkspace.js`) no longer has a Project Name field or a manual base-Price input. Selecting a Category auto-fetches a fixed base price from a new `categoryBasePriceModel` collection (managed at `/admin-panel/project-setup/base-price`, `AdminCategoryBasePricePage.js`). Additional Features are unchanged — still the existing `feature_upgrades`-category `productModel` products (`SummaryApi.allProduct`) — a first attempt at a separate dedicated feature collection was built then explicitly removed the same session once live DB data showed 8 real `feature_upgrades` products already exist. A Reference Total (Base + Features) line is shown for context; Selling Price remains fully manual and Discount = Reference Total − Selling Price. `adminCreateProjectOrder.js` re-derives base price (from `categoryBasePriceModel`) and feature prices (re-fetched from `productModel`, filtered to `feature_upgrades`) server-side, never trusting client-submitted values. See `35_CATEGORY_BASE_PRICE_AND_PROJECT_FEATURES_SYSTEM.md`.
- **`feature_upgrades` product management (current)**: admin can now list/create/edit/delete `feature_upgrades` products at `/admin-panel/project-setup/features` (`AdminFeatureProductsPage.js`, second entry in the `Project Setup` sidebar section), backed by a new list-only `GET /api/admin/feature-products` and the pre-existing generic `uploadProduct`/`updateProduct`/`deleteProduct` endpoints (unchanged, reused as-is). Previously no page anywhere covered this category. See `36_ADMIN_FEATURE_PRODUCTS_MANAGEMENT_PAGE.md`.
- Legacy plan-type creation UI (`UploadProduct.js`, `AllProducts.js`) is confirmed unrouted dead code — admin cannot create a new legacy-type plan through any UI path today. The one real live customer's 3 existing legacy plan orders have been additively migrated (not replaced) to also carry new `isServicePlan`/`servicePlanSnapshot`/`servicePlanStatus` fields on `orderProductModel.js`, via a dry-run-first, backup-first migration script (`backend/scripts/migrateLegacyPlansToServicePlan.js`). No enforcement engine reads these new fields yet — the customer's actual plan behavior is still driven entirely by legacy code, which has not been touched or removed. Read `27_SERVICE_PLAN_RENAME_AND_LEGACY_MIGRATION.md` before running that script again or before building the Service Plan enforcement/cycle engine.

## Notes

- `Header` is role-based and selects the admin or customer header automatically.
- Login currently uses direct sign-in with `postLogin()` redirecting to `/home`.
- `AdminDashboardDummy.js` is no longer part of the active codebase.
- `CustomerDashboard` is the active customer dashboard page.
- `ProjectsAndPlans` is the active project and plan tracking list.
- `OrderPage` is the active purchase-history list and should not be used for progress tracking.
- The customer sidebar `Start New Project` quick link is visible and points to `/start-new-project`, which now renders the new **`startproject.js`** intake-card page — **now 3 cards** (New Website Project / New Software Project / Features & Updates, reduced from an original 6 — see `32_...md`). Only **New Website Project** has a real target page: `/start-new-project/build/new_website` (`StartNewWebsiteBuild.js`, a branching Typeform-style question flow) and `/start-new-project/build/new_website/customize` (`StartNewWebsiteCustomize.js`, placeholder). Clicking **New Software Project** or **Features & Updates** opens a dark-glass "Coming Soon" modal instead of navigating — neither has a built target route. The old catalog page **`StartNewProject.js`** (live-wired to real product data via `GET /api/get-product`) and its detail page **`StartNewProjectDetail.js`** (fetches via `POST /api/product-details`, no "Proceed to Payment" handler) still exist in the codebase, fully functional, just disconnected from this route — restorable in one line in `customerRoutes.js`. See `15_START_NEW_PROJECT_UI_HISTORY.md` for the old page's history, `29_STARTPROJECT_INTAKE_PAGE_AND_PORTAL_GLASSMORPHISM.md` for the new page and why the swap happened, and `32_START_NEW_WEBSITE_QUESTION_FLOW_AND_DASHBOARD_HIGHLIGHT.md` for the New Website Project flow.
- **Plan-vs-project type detection**: centralized in `frontend/src/helpers/orderType.js` (`isPlanItem`/`isProjectItem`, recognizes both `website_updates` and `service_plan` categories) — `CustomerDashboard.js`, `OrderPage.js`, `ProjectsAndPlans.js`, `PaymentStatusChip.js` all import from here now, no per-file duplicates. `CustomerDashboard.js`'s `getItemLink` and `ProjectsAndPlans.js`'s `openDetails` both route plan orders to `/plan-details/:id` and project orders to `/project-details/:id`. As a second safety layer, `PlanDetails.js` and `ProjectDetails.js` (customer-side only, `isAdminView` untouched) each verify their fetched order's actual type after loading and self-redirect (`{ replace: true }`) to the correct sibling page on mismatch — so any current or future entry point, correct or not, always lands on the right detail page. See `32_...md` Sections 6-7 for the full root-cause investigation and fix.
- `frontend/src/components/CustomerWorkspaceTabs.js` is a shared underline-style tab component (mirrors admin's `AdminWorkspaceTabs.js`) used by `StartNewProject`, `ProjectsAndPlans`, `OrderPage`, and `UserInvoices`, replacing the earlier pill-style tab buttons on all four pages.
- `DashboardLayout` and `AdminLayout` sidebars use `sticky` positioning inside a flex row with the content column (not `position: fixed`), so the page footer runs full-width below both the sidebar and the content instead of only following content height.
- `WalletDetails` content container uses `max-w-7xl`, matching `ProjectsAndPlans` and `StartNewProject` widths.
- `ProjectsAndPlans` has only three tabs: `All`, `Projects`, `Plans`. Project row status is now derived from real order lifecycle fields (`Booked`, `Developer Assigned`, `{progress}% Complete`, `Completed`, `Payment Rejected`) instead of a static "In progress" label; see `00_CURRENT_SYSTEM.md` for the exact condition mapping. `Developer Assigned` is a static placeholder label, not a real developer-assignment feature — no backend endpoint for assigning a developer currently exists.
- `StartNewProjectDetail.js` now renders a new shared component, `frontend/src/components/ProjectDetailView.js`, instead of its own inline detail markup. `ProjectsAndPlans.js`, `StartNewProject.js`, `StartNewProjectDetail.js` (via `ProjectDetailView.js`), and the pre-existing `ProjectDetails.js` all now share the same dark-gradient-banner header style. A first admin-panel project detail page was built, explicitly rejected, and fully reverted — no admin detail/edit/delete page exists yet. See `18_PROJECT_DETAIL_PAGE_AND_HEADER_REWORK.md`.
- `ProjectsAndPlans.js`'s `openDetails()` now routes plan orders to a new page, `PlanDetails.js`, at `/plan-details/:orderId` — project orders still go to `ProjectDetails.js` at `/project-details/:orderId` unchanged. `PlanDetails.js` copies `ProjectDetails.js`'s 3-column layout skeleton but shows plan-specific content: a donut of updates-consumed-of-total, a plan snapshot (days left/resets-on, total updates, file limit), an update-request history list, and a per-file request-detail panel. Live-wired to `GET /api/order-details/:orderId` and `GET /api/get-update-requests` (client-side filtered by `updatePlanId._id`). `27_ADD_PROJECT...`-style dead-page cleanup, the `UserUpdateDashboard.js` plan-type bug fix, the one required `getOrderDetails.js` populate fix, and a confirmed-unfixed admin invoice/resume bug are all recorded in `20_PLAN_SYSTEM_AND_PLAN_DETAILS_PAGE.md` — read it before touching any of these files.
- Admin `Payment & Invoices` tab: the 2-unlinked-records-per-sale bug is root-caused and fixed for new records (admin's "Record Payment" action, renamed from "Mark Invoice Paid", now supports an admin-only `internalNote` never shown to the customer) and backfilled for the 7 pre-existing orphaned paid invoices. The customer-facing invoice/payment system itself (`/my-invoices`, `PlanDetails.js`'s/`UserUpdateDashboard.js`'s "Payment overdue" notices) is confirmed broken/missing end-to-end and still needs to be built. See `21_PAYMENT_INVOICE_LEDGER_AUDIT_AND_FIX.md` before touching any admin payment/invoice file or before starting the customer invoice feature.
- `OrderDetailPage.js` (`/order-detail/:orderId`) was reworked UI-only: no more status badge/Download Invoice/Track Project/Order Progress/Payment Summary; shows a simplified `Plan Snapshot` (Start/End/Due Date/Cycle, no order ID) side-by-side with a clickable `Invoice History` (any order with invoice records, not just recurring plans) or `Installments` card. New `InvoiceDetailPage.js` (`/invoice-detail/:invoiceId`) added for per-invoice detail/Download/Pay Now. **Contains a temporary, clearly-commented `DUMMY_INVOICES` fallback** shown only when an order has zero real invoices — most one-time-purchase and installment orders currently have zero real invoices because no backend code path creates a `monthlyInvoiceModel` document for them (confirmed via live DB query; only the monthly-recurring-plan cron creates invoices today). That backend gap is documented but explicitly not fixed yet. See `24_ORDER_DETAIL_INVOICE_UI_REWORK.md` before touching `OrderDetailPage.js`, `InvoiceDetailPage.js`, or before starting that backend work.
- `OrderDetailPage.js`/`InvoiceDetailPage.js` content width now matches the site standard `max-w-7xl` (was `max-w-6xl`/`max-w-3xl`). `OrderDetailPage.js`'s Back button now lives inside the dark header banner as a translucent pill (`PlanDetails.js` pattern), and the emerald "Plan"/"Order" eyebrow badge that used to sit below it has been removed. `OrderPage.js`'s header still uses the dark-gradient-banner-card style described here (eyebrow badge, white heading, `Total: {orders.length}` chip, banner-styled Refresh) — **`ProjectsAndPlans.js`'s header no longer matches this**, see the next note. See `25_ORDERS_PLANS_UI_AND_ADMIN_PLAN_LISTING.md`.
- **`ProjectsAndPlans.js` (current, supersedes the banner description above)**: the old single-card banner+table layout and the dark-gradient-banner header were both replaced. The heading is now a plain open block (no card, no eyebrow badge) matching `startproject.js`/`GamesListPage.js`. Below it is one `TicketsList.js`-style dark-glass card holding a combined title+filter-tabs+Total/Active/Refresh header row (wrapped in one row, not a separate row) and a table with a `bg-white/5` header band. `CustomerWorkspaceTabs.js` gained a `variant="inline"` prop (default unchanged for `OrderPage.js`/`StartNewProject.js`/`UserInvoices.js`) and a scrollbar-hiding fix shared by all 4 consumers. See `30_PROJECTS_AND_PLANS_UI_CONSISTENCY_REWORK.md`.
- `StartNewProject.js` now has a 4th tab, "Plans" (`website_updates` category, previously excluded), and no longer shows a "Who is it for?" column in its list (description is still shown; only the perfectFor tag row was removed). `StartNewProjectDetail.js`/`ProjectDetailView.js` were explicitly not touched by that change. Separately, `ProjectDetailView.js`'s section order changed to Description -> Who Is This For? -> What You Get -> Add More to Your Project (was Description -> What You Get -> Add More to Your Project -> Who Is This For?) — pure reorder. See `25_ORDERS_PLANS_UI_AND_ADMIN_PLAN_LISTING.md`, including Section 6's record of a rejected/reverted over-broad attempt at the tab+column change.
- **Portal-wide glassmorphism design system (current standard)**: nearly the whole customer portal now uses a real background image (`frontend/src/assets/BG.png`) with three reusable visual patterns — dark-glass cards (`bg-white/10 backdrop-blur-2xl`, white text, used for headings/banners/action cards), light-glass tables (`bg-white/55 backdrop-blur-xl`, **black text**, used for dense data like order/transaction lists — never white text here), and green "action-pill" buttons (`bg-emerald-500/20 border-emerald-400/40`, used for primary CTAs and success/completed status badges). Payment-input forms (Wallet recharge modal) are deliberately left solid white, not glassed. `DashboardLayout.js` and `SharedHeader.js` are both dark-themed as the shared foundation. Reuse these three patterns directly for any new customer page. See `29_STARTPROJECT_INTAKE_PAGE_AND_PORTAL_GLASSMORPHISM.md`.
- **`CustomerDashboard.js` (current, supersedes any earlier banner+metrics+table section description)**: same pattern as `ProjectsAndPlans.js` above. Open heading (`<h1>Dashboard</h1>`, no card, no badge), an open `MetricCard` grid (Live project / Wallet balance / Completed items / Open alerts, no wrapping section), and one `TicketsList.js`-style dark-glass card for "Recent projects & plans" (title+View-all-orders+Refresh in one header row, `bg-white/5` table-header band). The "Live project" `MetricCard` is dynamic: it reuses the pre-existing `primaryAction` logic to become a clickable link — "Track Project"/"Request Website Update"/"N Active Services" when an active project or plan exists, else "Start New Project" (`to='/start-new-project'`). No page-level loading skeleton exists anymore — a stale pre-dark-theme `if (loading)` block (light `#f8fafc` gradient + white pulse blocks) that caused a Dashboard-only white flash on every navigation was found and deleted, along with the then-dead `loading` state; the page now renders straight into the dark theme and briefly shows genuine empty/zero values until `/api/orders-list` resolves. Table rows on both this page and `ProjectsAndPlans.js` use `index % 2` alternating shading (`bg-white/[0.02]` / `bg-white/[0.06]`) so rows are visually distinguishable without hovering. See `30_PROJECTS_AND_PLANS_UI_CONSISTENCY_REWORK.md`.
- `DashboardLayout.js`'s sidebar: `quickLinks` is now Dashboard, Projects and Plans, Start New Project (previously also had Orders — see the superseding note below); `secondaryLinks` ("More") is now Wallet, Games, Profile, Support (previously had Orders moved out here — see below). See `30_PROJECTS_AND_PLANS_UI_CONSISTENCY_REWORK.md`.
- **`DashboardLayout.js` sidebar (current, supersedes the line above)**: `quickLinks` = Dashboard, Projects and Plans, Start New Project only (Orders removed). `secondaryLinks` ("More", also the mobile drawer content) = **Orders**, Wallet, Games, Profile, Support. A new `frontend/src/components/MobileBottomNav.js` (`fixed bottom-0 lg:hidden`, 5 columns) now renders below `lg` breakpoint: Dashboard / Projects / Start / Games / **More** (More opens the existing `MobileSidebarDrawer`, not a new panel). Desktop (`lg:flex`) sidebar and `MobileSidebarDrawer`'s content are otherwise unchanged. See `34_DETAIL_PAGE_GLASS_ROLLOUT_CHESS_REWORK_AND_MOBILE_BOTTOM_NAV.md`.
- **Customer custom-project order creation (current)**: the Customize page (`StartNewWebsiteCustomize.js`) creates a **product-less** custom project via `POST /api/customer/custom-project-order` (`customerCreateCustomProjectOrder.js`), the customer-side twin of `adminCreateProjectOrder.js`. Price is **always re-derived server-side** (`categoryBasePriceModel` base + compatible `feature_upgrades`; `Add New Page` = `sellingPrice × pageCount`, clamped 4…99) — the client never sends a trusted price. The order is created `pending-approval` (customer-initiated), gets a 0% timeline node, and is paid **in-page** (wallet `/wallet/deduct` + `/wallet/verify-payment`, or UPI QR → verify, always with `orderId` so no second order is created — the `InstallmentPayment.js` pattern, **not** `DirectPayment.js` which would duplicate). Partial payment uses customer-chosen 2 (50/50) or 3 (30/30/40) installments; only installment #1 is charged now. `DirectPayment.js`/`createOrder.js` are unchanged and are still the path for real catalog-product purchases. Coupon is not yet validated; no invoice is created by this endpoint. See `42_CUSTOMIZE_FLOW_BACKEND_WIRING_AND_INPAGE_PAYMENT.md` (builds on `41_...md`).
- **Full-page glass "documentation" layout (current default for form/detail pages)**: established on `StartNewWebsiteCustomize.js` and user-declared the standard to reuse. Pattern: open centered header outside any panel (site-standard `absolute left-0` detail-page Back pill + centered `<h1>`), then **one frameless full-page glass sheet** (`rounded-[2rem] bg-white/[0.06] p-6 backdrop-blur-2xl sm:p-8 lg:p-10`) with **no heavy border, no shadow, and NO `overflow-hidden`** — this is what stops it reading as a hard "box" and lets absolutely-positioned dropdowns/popovers escape without being clipped. Inside it a responsive documentation grid (`grid grid-cols-1 gap-10 lg:grid-cols-[minmax(0,1fr)_360px] lg:gap-14`: main content left, sticky summary/action rail right via `lg:sticky lg:top-6` + `lg:border-l lg:pl-14`; single stacked column on mobile), with plain `<section>`s separated by `border-t border-white/10 pt-10` dividers and small emerald eyebrow labels (`text-sm font-semibold uppercase tracking-wide text-emerald-300/90`) — no nested sub-cards. **Prefer this over the older single-bounding-card layout for any new customer-portal full-page form/detail page, and especially whenever the page has open dropdowns/popovers** (a bounding card's `overflow-hidden` clips them). The older `ProjectDetails.js` three-column bounding card stays valid for pages without escaping popovers. See `41_CUSTOMIZE_PROJECT_PAGE_AND_FULLPAGE_GLASS_SYSTEM.md`.
- **Chess (`frontend/src/chess/*`)**: now uses the same `BG.png` glass system and `DashboardLayout` sidebar as the rest of the customer portal (previously the only feature with neither). `ChessLobby.js` is a branching step-flow (mode select → Create/Join/Random, each with its own sub-steps), not a static all-at-once form. `useChessSocket.js` exposes a `players` state (`{white,black}`, each `{name,email}` once populated) for opponent-identity display; `ChessPage.js` shows "Playing against: {name} ({email})" during an active game and "vs {opponentName}" in the "Resume a game" list. Backend: `chessRoomManager.js`'s `getPlayerColor()` now handles both populated (`{_id,name,email}`) and raw `ObjectId` player references — a real bug where it silently misidentified color/opponent after populate was introduced was found and fixed here. See `34_DETAIL_PAGE_GLASS_ROLLOUT_CHESS_REWORK_AND_MOBILE_BOTTOM_NAV.md`.
